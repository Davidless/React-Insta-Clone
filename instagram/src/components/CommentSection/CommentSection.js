import React from "react";

const CommentSection = props => {
  return (
    <div>
      {/* {props.comments.map(
        (c, i) => (
          <ul key={i} c={c} />
        ),
        console.log(props)
      )} */}
    </div>
  );
};

export default CommentSection;
